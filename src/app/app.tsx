export function App() {
  return (
    <main className="grid min-h-screen place-items-center p-6">
      <section className="max-w-xl text-center">
        <p className="text-sm font-medium uppercase tracking-wider">Phase 1</p>
        <h1 className="mt-3 text-3xl font-semibold">
          HelloClient sidebar scaffold is ready
        </h1>
        <p className="mt-4 text-base leading-7">
          React, TypeScript, Tailwind CSS, and the test environment are
          configured. Sidebar behavior starts in a later phase.
        </p>
      </section>
    </main>
  )
}
